# PyInstaller
build/
dist/
*.spec

# Executável gerado na raiz do projeto (oficial: dist/ ou release/ após o build)
/*.exe

# Python
__pycache__/
*.py[cod]
*$py.class

# Release local (binários grandes — remova esta linha se quiser versionar .exe no Git)
release/
